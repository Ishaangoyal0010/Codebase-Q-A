version: "3.9"

services:

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: your-dockerhub-username/codebaseqa-backend:latest
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    image: your-dockerhub-username/codebaseqa-frontend:latest
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      - backend
